import {createStyles} from 'antd-style'

export default createStyles(({token}) => ({
  container: {display: 'relative', width: '100%', overflow: 'auto'},
  table: {width: '100%', captionSide: 'bottom', fontSize: '14px', }
}))